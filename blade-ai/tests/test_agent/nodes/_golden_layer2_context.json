[
  {
    "kind": "context",
    "label": "node-disk-fill-imagefs-small-toolpod-baseline",
    "out": "## Layer 1 Result (already completed)\nLayer 1 for experiment uid-1: passed\nDetails: Success\n\n\n## Resource Coverage (from blade_query_k8s)\nAffected resources: 1\nTarget resources from context: ['test-node']\n\nCompare affected_count against the expected number of target resources. If fewer resources are affected than expected, the fault injection has INCOMPLETE COVERAGE. Investigate and report this in VERIFICATION_RESULT Warnings.\n## Fault Context\nSkill: node-disk-fill\nTarget namespace: default\nTarget names: ['test-node']\nBlade params: {}\nKubeconfig: /kc\nInjection key parameters: {'path': '/var/lib/docker', 'size': '1000'}\nScope: node | Target: disk | Action: fill\n\n## Available Tool Pod\nA tool pod is available for cluster-level operations:\n- Pod name: `tool-pod-a`\n- Namespace: unknown — identify it across all namespaces before exec (the namespace is deployment-specific; never assume it)\n- Capabilities: injection-tool commands (status/destroy), cluster API checks (describe/top/get), and host-level checks via `/host/...` (the tool pod typically mounts the host root).\n- For CRD-mode disk fill, the fill file IS in the container overlay — checking it inside this pod is the PRIMARY verification method.\n- For host filesystem verification (e.g., `/proc/loadavg`, `/var/log`), prefix paths with `/host` (try `/host/proc/loadavg` first; if missing, fall back to bare `/proc/loadavg`). If this tool pod is unavailable or lacks /host access, fall back to a node debug pod (busybox image) and exec into it.\n- **UID Dual Mapping**: The experiment UID (uid-1) is the CRD resource name. Inside the tool pod, the injection tool's local status subcommand searches the LOCAL experiment database and typically returns 'record not found' for an experiment created through the cluster API — NEVER use it for this check (it causes a false-negative Layer 2 conclusion). Query the experiment CRD through the cluster API instead (discover the experiment resource kind via the cluster query tool itself; knowledge docs provide reference forms).\n\n## Injection Verification Instructions\nNo skill use-case content is available. Design verification based on the fault-specific hints below and your expertise.\nBefore your final conclusion, output a **Verification Checklist** listing each check and its result:\n   - Check 1: passed/failed/skipped — brief evidence\n   - ...\n**WARNING**: Without skill guidance, verification may be incomplete. At minimum, verify the fault effect is observable on the target.\n**Knowledge docs**: Check the Domain Knowledge Index for documents whose \"When to read\" field covers your current scenario (e.g., verification strategies, kubectl field reference). Use `read_knowledge_resource` to load them before designing your verification plan.\n\nLayer 1 is PASSED. Now perform Layer 2 verification: use the available observation tools to verify the fault is actually in effect on the target.\n**NEGATIVE EVIDENCE ENUMERATION (CRITICAL)**:\nBefore concluding Layer2 'passed', you MUST include a 'Negative Evidence' section in your reasoning that explicitly lists EVERY observation contradicting or weakening the conclusion that the fault is in effect. For each item, either:\n(a) Dismiss it with factual basis (not speculation), or\n(b) Accept it as valid counter-evidence.\nIf ANY element of the effect claim (a significant change observed, attributable to the injection) is demonstrably NOT met, you MUST conclude Layer2 as 'partial' or 'failed' — NOT 'passed'. (Absence of propagated effects (OOM, latency, business impact) is NOT counter-evidence against the fault being in effect.)\n\n**EVIDENCE SEMANTICS (CRITICAL)**: what each observation means.\n- Qualitative faults (process gone / NotReady / unreachable): the observation is binary — a clear one settles the step.\n- Quantitative faults: the injected value (e.g. --mem-percent 80) is a mechanism parameter, not a measurement promise. The effect evidence is a significant change from baseline — or, with no baseline, significant deviation from the expected healthy state; a gap vs the injected value is magnitude information for Warnings.\n- Mechanism evidence (experiment registration, tool receipts, rule snapshots, unit liveness) speaks for the mechanism, not the outcome — field-proven: tools have reported Success while delivering no fault. When the effect is missing, it points to the failed layer (command not issued / mechanism not alive / payload spinning / effect not propagated).\n- Derived status labels (e.g. CrashLoopBackOff) render underlying quantities — restarts climbing, back-off events; the quantities are the evidence, the label their display form.\n- Fault effects propagate with physical delay (heartbeat grace, probe periods, image pull); an observation that predates propagation says nothing yet about the outcome.\n\n\n### Fault-Specific Verification Hints\nFault metadata: Scope: node | Target: disk | Action: fill\nFault scenario: node-disk fill\nFor 'disk' verification specifics (observation methods, partition/overlay or protocol semantics, data-interpretation pitfalls), load the relevant knowledge doc via `read_knowledge_resource` — e.g. `fault-verification-strategies.md`.\nEvidence sufficiency is your holistic judgment. A terminal/unreachable primary state can be conclusive on its own — do not probe into an unreachable target to collect an extra cross-metric (it only times out). If a cross-check helps, run it from a healthy peer.\n\n**BASELINE INTEGRITY** (applies to ALL quantitative metric verification — disk %, CPU %, memory %, latency ms, etc. Does NOT apply to qualitative status checks like 'Pod is Running' or 'Service is reachable'):\n1. IDENTIFY the exact resource you are measuring — be specific:\n   \"imagefs /dev/vdb\", \"node cn-hongkong.10.0.2.69 CPU\", \"pod accounting memory\", \"endpoint /api/health latency\"\n   \"disk\" or \"CPU\" alone is ambiguous — always include the resource identity.\n2. When no pre-injection baseline exists for a resource, your FIRST measurement of it is your baseline for later re-checks — record the resource identity AND value together.\n3. ALL comparisons MUST be against the SAME resource. NEVER compare metrics from different resources:\n   ✅ \"imagefs /dev/vdb: first-check 42% → re-check 84%\" (same partition, valid delta)\n   ✅ \"node X CPU: 12% → 89%\" (same node, valid delta)\n   ❌ \"first-check 16% → re-check 84%\" (different partitions: 16% was nodefs /dev/vda3, 84% was imagefs /dev/vdb — INVALID comparison)\n4. If you lack a pre-injection baseline for the target resource, say so explicitly:\n   \"No pre-injection baseline available for imagefs /dev/vdb. Current value: 84%.\"\n5. HIGH post-injection values WITHOUT baseline context are ambiguous — the value may be pre-existing, not fault-caused. Look for corroborating evidence (e.g., DiskPressure condition, recent events, timestamp correlation with injection time).\n6. If your first-check value already matches the expected injection parameter (e.g., --percent 85 → first-check shows 84%), this IS evidence the fault is in effect — do NOT conclude 'no change' just because re-check shows the same value; a drift toward the target that is not yet significant is a trend, not attainment (attainment is the significant change itself — see EVIDENCE SEMANTICS).\n7. EXPECTED NEGATIVE RESULTS: If the PRIMARY metric confirms the fault is in effect (e.g., disk usage rose significantly from baseline), but a THRESHOLD-DEPENDENT condition is not met (e.g., DiskPressure=False because usage is 84% vs 85% threshold), mark that step as 'expected' — the negative result is anticipated and does not indicate injection failure. Do NOT use 'expected' as a synonym for 'failed'.\n\n**Layer 1 Limitation**: Layer 1 only checks whether the fault experiment is registered. It does NOT verify that the fault effect is observable. Your Layer 2 verification is the ONLY way to confirm the fault is working.\n\n**NOTE**: Some minimal container images lack common shell utilities (top, ps, netstat, etc.). If a container exec check returns empty output or \"command not found\", do NOT retry — use a cluster API describe-style check instead.\n\n### Test Pod Namespace Selection\nWhen you need a Running application pod for verification tests (DNS resolution, network connectivity, service calls), do NOT only search the `default` namespace. Search in the TARGET namespace first (default), then other namespaces with workloads. For cluster-wide faults (DNS, node-level), any Running pod with shell access can serve as a test target.\n\n### Debug Pod Cleanup\nIf you create any pods during verification (e.g., temporary test pods), you MUST delete them before finishing verification. Add cleanup as a final step in your checklist.\nNote: framework-managed host-access pods (the carrier tool pods surfaced in the hints above) are DaemonSet-managed and MUST NOT be deleted.\n"
  },
  {
    "kind": "context",
    "label": "node-disk-fill-nodefs-large-notoolpod",
    "out": "## Layer 1 Result (already completed)\nLayer 1 for experiment uid-2: passed\nDetails: Success\n\n\n## Resource Coverage (from blade_query_k8s)\nAffected resources: 1\nTarget resources from context: ['test-node']\n\nCompare affected_count against the expected number of target resources. If fewer resources are affected than expected, the fault injection has INCOMPLETE COVERAGE. Investigate and report this in VERIFICATION_RESULT Warnings.\n## Fault Context\nSkill: node-disk-fill\nTarget namespace: default\nTarget names: ['test-node']\nBlade params: {}\nKubeconfig: /kc\nInjection key parameters: {'path': '/var/log', 'size': '10000'}\nScope: node | Target: disk | Action: fill\n\n## Baseline Data Note\nBaseline capture was attempted but produced no usable data. You MUST be cautious when interpreting absolute metric values — high resource usage does NOT necessarily mean the fault is in effect. Cross-validate with multiple independent data points (metrics + events + conditions) before concluding 'verified'.\n\n## Injection Verification Instructions\nNo skill use-case content is available. Design verification based on the fault-specific hints below and your expertise.\nBefore your final conclusion, output a **Verification Checklist** listing each check and its result:\n   - Check 1: passed/failed/skipped — brief evidence\n   - ...\n**WARNING**: Without skill guidance, verification may be incomplete. At minimum, verify the fault effect is observable on the target.\n**Knowledge docs**: Check the Domain Knowledge Index for documents whose \"When to read\" field covers your current scenario (e.g., verification strategies, kubectl field reference). Use `read_knowledge_resource` to load them before designing your verification plan.\n\nLayer 1 is PASSED. Now perform Layer 2 verification: use the available observation tools to verify the fault is actually in effect on the target.\n**NEGATIVE EVIDENCE ENUMERATION (CRITICAL)**:\nBefore concluding Layer2 'passed', you MUST include a 'Negative Evidence' section in your reasoning that explicitly lists EVERY observation contradicting or weakening the conclusion that the fault is in effect. For each item, either:\n(a) Dismiss it with factual basis (not speculation), or\n(b) Accept it as valid counter-evidence.\nIf ANY element of the effect claim (a significant change observed, attributable to the injection) is demonstrably NOT met, you MUST conclude Layer2 as 'partial' or 'failed' — NOT 'passed'. (Absence of propagated effects (OOM, latency, business impact) is NOT counter-evidence against the fault being in effect.)\n\n**EVIDENCE SEMANTICS (CRITICAL)**: what each observation means.\n- Qualitative faults (process gone / NotReady / unreachable): the observation is binary — a clear one settles the step.\n- Quantitative faults: the injected value (e.g. --mem-percent 80) is a mechanism parameter, not a measurement promise. The effect evidence is a significant change from baseline — or, with no baseline, significant deviation from the expected healthy state; a gap vs the injected value is magnitude information for Warnings.\n- Mechanism evidence (experiment registration, tool receipts, rule snapshots, unit liveness) speaks for the mechanism, not the outcome — field-proven: tools have reported Success while delivering no fault. When the effect is missing, it points to the failed layer (command not issued / mechanism not alive / payload spinning / effect not propagated).\n- Derived status labels (e.g. CrashLoopBackOff) render underlying quantities — restarts climbing, back-off events; the quantities are the evidence, the label their display form.\n- Fault effects propagate with physical delay (heartbeat grace, probe periods, image pull); an observation that predates propagation says nothing yet about the outcome.\n\n\n### Fault-Specific Verification Hints\nFault metadata: Scope: node | Target: disk | Action: fill\nFault scenario: node-disk fill\nFor 'disk' verification specifics (observation methods, partition/overlay or protocol semantics, data-interpretation pitfalls), load the relevant knowledge doc via `read_knowledge_resource` — e.g. `fault-verification-strategies.md`.\nEvidence sufficiency is your holistic judgment. A terminal/unreachable primary state can be conclusive on its own — do not probe into an unreachable target to collect an extra cross-metric (it only times out). If a cross-check helps, run it from a healthy peer.\n\n**Layer 1 Limitation**: Layer 1 only checks whether the fault experiment is registered. It does NOT verify that the fault effect is observable. Your Layer 2 verification is the ONLY way to confirm the fault is working.\n\n**NOTE**: Some minimal container images lack common shell utilities (top, ps, netstat, etc.). If a container exec check returns empty output or \"command not found\", do NOT retry — use a cluster API describe-style check instead.\n\n### Test Pod Namespace Selection\nWhen you need a Running application pod for verification tests (DNS resolution, network connectivity, service calls), do NOT only search the `default` namespace. Search in the TARGET namespace first (default), then other namespaces with workloads. For cluster-wide faults (DNS, node-level), any Running pod with shell access can serve as a test target.\n\n### Debug Pod Cleanup\nIf you create any pods during verification (e.g., temporary test pods), you MUST delete them before finishing verification. Add cleanup as a final step in your checklist.\nNote: framework-managed host-access pods (the carrier tool pods surfaced in the hints above) are DaemonSet-managed and MUST NOT be deleted.\n"
  },
  {
    "kind": "context",
    "label": "node-disk-fill-unknownpath-toolpod",
    "out": "## Layer 1 Result (already completed)\nLayer 1 for experiment uid-3: passed\nDetails: Success\n\n\n## Resource Coverage (from blade_query_k8s)\nAffected resources: 1\nTarget resources from context: ['test-node']\n\nCompare affected_count against the expected number of target resources. If fewer resources are affected than expected, the fault injection has INCOMPLETE COVERAGE. Investigate and report this in VERIFICATION_RESULT Warnings.\n## Fault Context\nSkill: node-disk-fill\nTarget namespace: default\nTarget names: ['test-node']\nBlade params: {}\nKubeconfig: /kc\nInjection key parameters: {'path': '/weird/place', 'size': '6000'}\nScope: node | Target: disk | Action: fill\n\n## Baseline Data Note\nBaseline capture was attempted but produced no usable data. You MUST be cautious when interpreting absolute metric values — high resource usage does NOT necessarily mean the fault is in effect. Cross-validate with multiple independent data points (metrics + events + conditions) before concluding 'verified'.\n\n## Available Tool Pod\nA tool pod is available for cluster-level operations:\n- Pod name: `tool-pod-c`\n- Namespace: unknown — identify it across all namespaces before exec (the namespace is deployment-specific; never assume it)\n- Capabilities: injection-tool commands (status/destroy), cluster API checks (describe/top/get), and host-level checks via `/host/...` (the tool pod typically mounts the host root).\n- For CRD-mode disk fill, the fill file IS in the container overlay — checking it inside this pod is the PRIMARY verification method.\n- For host filesystem verification (e.g., `/proc/loadavg`, `/var/log`), prefix paths with `/host` (try `/host/proc/loadavg` first; if missing, fall back to bare `/proc/loadavg`). If this tool pod is unavailable or lacks /host access, fall back to a node debug pod (busybox image) and exec into it.\n- **UID Dual Mapping**: The experiment UID (uid-3) is the CRD resource name. Inside the tool pod, the injection tool's local status subcommand searches the LOCAL experiment database and typically returns 'record not found' for an experiment created through the cluster API — NEVER use it for this check (it causes a false-negative Layer 2 conclusion). Query the experiment CRD through the cluster API instead (discover the experiment resource kind via the cluster query tool itself; knowledge docs provide reference forms).\n\n## Injection Verification Instructions\nNo skill use-case content is available. Design verification based on the fault-specific hints below and your expertise.\nBefore your final conclusion, output a **Verification Checklist** listing each check and its result:\n   - Check 1: passed/failed/skipped — brief evidence\n   - ...\n**WARNING**: Without skill guidance, verification may be incomplete. At minimum, verify the fault effect is observable on the target.\n**Knowledge docs**: Check the Domain Knowledge Index for documents whose \"When to read\" field covers your current scenario (e.g., verification strategies, kubectl field reference). Use `read_knowledge_resource` to load them before designing your verification plan.\n\nLayer 1 is PASSED. Now perform Layer 2 verification: use the available observation tools to verify the fault is actually in effect on the target.\n**NEGATIVE EVIDENCE ENUMERATION (CRITICAL)**:\nBefore concluding Layer2 'passed', you MUST include a 'Negative Evidence' section in your reasoning that explicitly lists EVERY observation contradicting or weakening the conclusion that the fault is in effect. For each item, either:\n(a) Dismiss it with factual basis (not speculation), or\n(b) Accept it as valid counter-evidence.\nIf ANY element of the effect claim (a significant change observed, attributable to the injection) is demonstrably NOT met, you MUST conclude Layer2 as 'partial' or 'failed' — NOT 'passed'. (Absence of propagated effects (OOM, latency, business impact) is NOT counter-evidence against the fault being in effect.)\n\n**EVIDENCE SEMANTICS (CRITICAL)**: what each observation means.\n- Qualitative faults (process gone / NotReady / unreachable): the observation is binary — a clear one settles the step.\n- Quantitative faults: the injected value (e.g. --mem-percent 80) is a mechanism parameter, not a measurement promise. The effect evidence is a significant change from baseline — or, with no baseline, significant deviation from the expected healthy state; a gap vs the injected value is magnitude information for Warnings.\n- Mechanism evidence (experiment registration, tool receipts, rule snapshots, unit liveness) speaks for the mechanism, not the outcome — field-proven: tools have reported Success while delivering no fault. When the effect is missing, it points to the failed layer (command not issued / mechanism not alive / payload spinning / effect not propagated).\n- Derived status labels (e.g. CrashLoopBackOff) render underlying quantities — restarts climbing, back-off events; the quantities are the evidence, the label their display form.\n- Fault effects propagate with physical delay (heartbeat grace, probe periods, image pull); an observation that predates propagation says nothing yet about the outcome.\n\n\n### Fault-Specific Verification Hints\nFault metadata: Scope: node | Target: disk | Action: fill\nFault scenario: node-disk fill\nFor 'disk' verification specifics (observation methods, partition/overlay or protocol semantics, data-interpretation pitfalls), load the relevant knowledge doc via `read_knowledge_resource` — e.g. `fault-verification-strategies.md`.\nEvidence sufficiency is your holistic judgment. A terminal/unreachable primary state can be conclusive on its own — do not probe into an unreachable target to collect an extra cross-metric (it only times out). If a cross-check helps, run it from a healthy peer.\n\n**Layer 1 Limitation**: Layer 1 only checks whether the fault experiment is registered. It does NOT verify that the fault effect is observable. Your Layer 2 verification is the ONLY way to confirm the fault is working.\n\n**NOTE**: Some minimal container images lack common shell utilities (top, ps, netstat, etc.). If a container exec check returns empty output or \"command not found\", do NOT retry — use a cluster API describe-style check instead.\n\n### Test Pod Namespace Selection\nWhen you need a Running application pod for verification tests (DNS resolution, network connectivity, service calls), do NOT only search the `default` namespace. Search in the TARGET namespace first (default), then other namespaces with workloads. For cluster-wide faults (DNS, node-level), any Running pod with shell access can serve as a test target.\n\n### Debug Pod Cleanup\nIf you create any pods during verification (e.g., temporary test pods), you MUST delete them before finishing verification. Add cleanup as a final step in your checklist.\nNote: framework-managed host-access pods (the carrier tool pods surfaced in the hints above) are DaemonSet-managed and MUST NOT be deleted.\n"
  },
  {
    "kind": "context",
    "label": "pod-disk-fill-small",
    "out": "## Layer 1 Result (already completed)\nLayer 1 for experiment uid-4: passed\nDetails: Success\n\n\n## Resource Coverage (from blade_query_k8s)\nAffected resources: 1\nTarget resources from context: ['test-node']\n\nCompare affected_count against the expected number of target resources. If fewer resources are affected than expected, the fault injection has INCOMPLETE COVERAGE. Investigate and report this in VERIFICATION_RESULT Warnings.\n## Fault Context\nSkill: pod-disk-fill\nTarget namespace: default\nTarget names: ['test-node']\nBlade params: {}\nKubeconfig: /kc\nInjection key parameters: {'path': '/tmp', 'size': '500'}\nScope: pod | Target: disk | Action: fill\n\n## Injection Verification Instructions\nNo skill use-case content is available. Design verification based on the fault-specific hints below and your expertise.\nBefore your final conclusion, output a **Verification Checklist** listing each check and its result:\n   - Check 1: passed/failed/skipped — brief evidence\n   - ...\n**WARNING**: Without skill guidance, verification may be incomplete. At minimum, verify the fault effect is observable on the target.\n**Knowledge docs**: Check the Domain Knowledge Index for documents whose \"When to read\" field covers your current scenario (e.g., verification strategies, kubectl field reference). Use `read_knowledge_resource` to load them before designing your verification plan.\n\nLayer 1 is PASSED. Now perform Layer 2 verification: use the available observation tools to verify the fault is actually in effect on the target.\n**NEGATIVE EVIDENCE ENUMERATION (CRITICAL)**:\nBefore concluding Layer2 'passed', you MUST include a 'Negative Evidence' section in your reasoning that explicitly lists EVERY observation contradicting or weakening the conclusion that the fault is in effect. For each item, either:\n(a) Dismiss it with factual basis (not speculation), or\n(b) Accept it as valid counter-evidence.\nIf ANY element of the effect claim (a significant change observed, attributable to the injection) is demonstrably NOT met, you MUST conclude Layer2 as 'partial' or 'failed' — NOT 'passed'. (Absence of propagated effects (OOM, latency, business impact) is NOT counter-evidence against the fault being in effect.)\n\n**EVIDENCE SEMANTICS (CRITICAL)**: what each observation means.\n- Qualitative faults (process gone / NotReady / unreachable): the observation is binary — a clear one settles the step.\n- Quantitative faults: the injected value (e.g. --mem-percent 80) is a mechanism parameter, not a measurement promise. The effect evidence is a significant change from baseline — or, with no baseline, significant deviation from the expected healthy state; a gap vs the injected value is magnitude information for Warnings.\n- Mechanism evidence (experiment registration, tool receipts, rule snapshots, unit liveness) speaks for the mechanism, not the outcome — field-proven: tools have reported Success while delivering no fault. When the effect is missing, it points to the failed layer (command not issued / mechanism not alive / payload spinning / effect not propagated).\n- Derived status labels (e.g. CrashLoopBackOff) render underlying quantities — restarts climbing, back-off events; the quantities are the evidence, the label their display form.\n- Fault effects propagate with physical delay (heartbeat grace, probe periods, image pull); an observation that predates propagation says nothing yet about the outcome.\n\n\n### Fault-Specific Verification Hints\nFault metadata: Scope: pod | Target: disk | Action: fill\nFault scenario: pod-disk fill\nFor 'disk' verification specifics (observation methods, partition/overlay or protocol semantics, data-interpretation pitfalls), load the relevant knowledge doc via `read_knowledge_resource` — e.g. `fault-verification-strategies.md`.\nEvidence sufficiency is your holistic judgment. A terminal/unreachable primary state can be conclusive on its own — do not probe into an unreachable target to collect an extra cross-metric (it only times out). If a cross-check helps, run it from a healthy peer.\n\n**BASELINE INTEGRITY** (applies to ALL quantitative metric verification — disk %, CPU %, memory %, latency ms, etc. Does NOT apply to qualitative status checks like 'Pod is Running' or 'Service is reachable'):\n1. IDENTIFY the exact resource you are measuring — be specific:\n   \"imagefs /dev/vdb\", \"node cn-hongkong.10.0.2.69 CPU\", \"pod accounting memory\", \"endpoint /api/health latency\"\n   \"disk\" or \"CPU\" alone is ambiguous — always include the resource identity.\n2. When no pre-injection baseline exists for a resource, your FIRST measurement of it is your baseline for later re-checks — record the resource identity AND value together.\n3. ALL comparisons MUST be against the SAME resource. NEVER compare metrics from different resources:\n   ✅ \"imagefs /dev/vdb: first-check 42% → re-check 84%\" (same partition, valid delta)\n   ✅ \"node X CPU: 12% → 89%\" (same node, valid delta)\n   ❌ \"first-check 16% → re-check 84%\" (different partitions: 16% was nodefs /dev/vda3, 84% was imagefs /dev/vdb — INVALID comparison)\n4. If you lack a pre-injection baseline for the target resource, say so explicitly:\n   \"No pre-injection baseline available for imagefs /dev/vdb. Current value: 84%.\"\n5. HIGH post-injection values WITHOUT baseline context are ambiguous — the value may be pre-existing, not fault-caused. Look for corroborating evidence (e.g., DiskPressure condition, recent events, timestamp correlation with injection time).\n6. If your first-check value already matches the expected injection parameter (e.g., --percent 85 → first-check shows 84%), this IS evidence the fault is in effect — do NOT conclude 'no change' just because re-check shows the same value; a drift toward the target that is not yet significant is a trend, not attainment (attainment is the significant change itself — see EVIDENCE SEMANTICS).\n7. EXPECTED NEGATIVE RESULTS: If the PRIMARY metric confirms the fault is in effect (e.g., disk usage rose significantly from baseline), but a THRESHOLD-DEPENDENT condition is not met (e.g., DiskPressure=False because usage is 84% vs 85% threshold), mark that step as 'expected' — the negative result is anticipated and does not indicate injection failure. Do NOT use 'expected' as a synonym for 'failed'.\n\n**Layer 1 Limitation**: Layer 1 only checks whether the fault experiment is registered. It does NOT verify that the fault effect is observable. Your Layer 2 verification is the ONLY way to confirm the fault is working.\n\n**NOTE**: Some minimal container images lack common shell utilities (top, ps, netstat, etc.). If a container exec check returns empty output or \"command not found\", do NOT retry — use a cluster API describe-style check instead.\n\n### Test Pod Namespace Selection\nWhen you need a Running application pod for verification tests (DNS resolution, network connectivity, service calls), do NOT only search the `default` namespace. Search in the TARGET namespace first (default), then other namespaces with workloads. For cluster-wide faults (DNS, node-level), any Running pod with shell access can serve as a test target.\n\n"
  },
  {
    "kind": "context",
    "label": "node-disk-burn-toolpod",
    "out": "## Layer 1 Result (already completed)\nLayer 1 for experiment uid-5: passed\nDetails: Success\n\n\n## Resource Coverage (from blade_query_k8s)\nAffected resources: 1\nTarget resources from context: ['test-node']\n\nCompare affected_count against the expected number of target resources. If fewer resources are affected than expected, the fault injection has INCOMPLETE COVERAGE. Investigate and report this in VERIFICATION_RESULT Warnings.\n## Fault Context\nSkill: node-disk-burn\nTarget namespace: default\nTarget names: ['test-node']\nBlade params: {}\nKubeconfig: /kc\nInjection key parameters: {'size': '1024'}\nScope: node | Target: disk | Action: burn\n\n## Baseline Data Note\nBaseline capture was attempted but produced no usable data. You MUST be cautious when interpreting absolute metric values — high resource usage does NOT necessarily mean the fault is in effect. Cross-validate with multiple independent data points (metrics + events + conditions) before concluding 'verified'.\n\n## Available Tool Pod\nA tool pod is available for cluster-level operations:\n- Pod name: `tool-pod-e`\n- Namespace: unknown — identify it across all namespaces before exec (the namespace is deployment-specific; never assume it)\n- Capabilities: injection-tool commands (status/destroy), cluster API checks (describe/top/get), and host-level checks via `/host/...` (the tool pod typically mounts the host root).\n- For CRD-mode disk fill, the fill file IS in the container overlay — checking it inside this pod is the PRIMARY verification method.\n- For host filesystem verification (e.g., `/proc/loadavg`, `/var/log`), prefix paths with `/host` (try `/host/proc/loadavg` first; if missing, fall back to bare `/proc/loadavg`). If this tool pod is unavailable or lacks /host access, fall back to a node debug pod (busybox image) and exec into it.\n- **UID Dual Mapping**: The experiment UID (uid-5) is the CRD resource name. Inside the tool pod, the injection tool's local status subcommand searches the LOCAL experiment database and typically returns 'record not found' for an experiment created through the cluster API — NEVER use it for this check (it causes a false-negative Layer 2 conclusion). Query the experiment CRD through the cluster API instead (discover the experiment resource kind via the cluster query tool itself; knowledge docs provide reference forms).\n\n## Disk Burn I/O Pre-Check (AUTHORITATIVE)\nThe injection engine programmatically verified the burn I/O effect on target node `n1` via tool pod `tp`.\nProgrammatic check confirmed: disk burn I/O is ACTIVE.\nWrite throughput on partition(s): /dev/vdb: ~120 MB/s\nThis is DEFINITIVE evidence that the disk burn fault is in effect — the dd processes are actively writing to the container overlay.\nThe I/O appears on the overlay's backing partition (typically imagefs, e.g. /dev/vdb), NOT on the nodefs partition (e.g. /dev/vda3) where /host/tmp resides. This is expected: CRD-mode burn writes to the container overlay, not the host filesystem.\nDO NOT conclude \"failed\" based on /host/tmp/ having no burn files or nodefs (vda3) showing no I/O — the burn is on a DIFFERENT partition.\nThe measured write throughput above is the effect evidence for the disk I/O verification step.\n\n## Injection Verification Instructions\nThe following skill use-case defines how to verify this fault. You MUST follow its verification approach as the primary reference.\n\n<skill-case>\n## 注入验证\n1. check dd\n\n</skill-case>\n\n### Verification Strategy (Template)\nSteps from the skill case:\n- Step 1: [status] — check dd\n\nStep handling:\n- Steps observing the injection itself taking effect on the target decide the verdict — they are your priority.\n- Steps describing propagated effects (OOM/eviction, latency, business impact) are NOT verdict criteria: if evidence is already in hand, record it faithfully (absence included); otherwise mark 'expected' or 'not_applicable' — NEVER add waiting, retries or extra sampling to observe them. If the step's target cannot be instantiated here (placeholder like '应用 A'), mark it 'not_applicable' with the reason; do NOT fabricate a target.\n\nRules:\n1. Line format: `Step N: <status> — <evidence>`,\n   keeping the case's step numbering.\n2. <status> ∈ passed, partial, failed, skipped, recovered_before_observation, expected, not_applicable.\n3. Every status needs evidence; for injection-effect steps `expected` without an observation is invalid — \n   use `skipped` if unchecked.\n4. ANSWER all 1 steps — any status with a\n   reason counts; silent omission is the only violation.\n5. Verdict: 'passed' requires ALL injection-effect steps\n   passed; a failed injection-effect step means the fault\n   may not be in effect ('failed'/'partial').\n6. Different method than specified? Note '(deviation: <why>)'.\n**Checklist Status Choice**:\n- The checklist reports OBSERVED FACTS, not predictions.\n- Steps observing the injection effect: did you perform the check? Yes → 'passed'/'failed' by what you OBSERVED; No → 'skipped'.\n- 'expected': the phenomenon is absent and the actual injection parameters make that the anticipated outcome. For propagated-effect steps (OOM, latency, business impact) this is valid WITHOUT observation; for injection-effect steps it requires the observation.\n- 'not_applicable': the step's target cannot exist in this environment (placeholder target, unnamed service). Do NOT fabricate a target.\n- Timing uncertainty belongs in Warnings, not in checklist status.\n- 'recovered_before_observation': the fault was transient and had dissipated by the time you checked — distinct from 'failed' (checked, fault absent).\n\nNote — Layer boundary: VERIFICATION_CHECKLIST must ONLY contain Layer 2 checks (observable fault effects). Do NOT include Layer 1 items (Layer 1 tool check, experiment registration).\n\nLayer 1 is PASSED. Now perform Layer 2 verification: use the available observation tools to verify the fault is actually in effect on the target.\n**NEGATIVE EVIDENCE ENUMERATION (CRITICAL)**:\nBefore concluding Layer2 'passed', you MUST include a 'Negative Evidence' section in your reasoning that explicitly lists EVERY observation contradicting or weakening the conclusion that the fault is in effect. For each item, either:\n(a) Dismiss it with factual basis (not speculation), or\n(b) Accept it as valid counter-evidence.\nIf ANY element of the effect claim (a significant change observed, attributable to the injection) is demonstrably NOT met, you MUST conclude Layer2 as 'partial' or 'failed' — NOT 'passed'. (Absence of propagated effects (OOM, latency, business impact) is NOT counter-evidence against the fault being in effect.)\n\n**EVIDENCE SEMANTICS (CRITICAL)**: what each observation means.\n- Qualitative faults (process gone / NotReady / unreachable): the observation is binary — a clear one settles the step.\n- Quantitative faults: the injected value (e.g. --mem-percent 80) is a mechanism parameter, not a measurement promise. The effect evidence is a significant change from baseline — or, with no baseline, significant deviation from the expected healthy state; a gap vs the injected value is magnitude information for Warnings.\n- Mechanism evidence (experiment registration, tool receipts, rule snapshots, unit liveness) speaks for the mechanism, not the outcome — field-proven: tools have reported Success while delivering no fault. When the effect is missing, it points to the failed layer (command not issued / mechanism not alive / payload spinning / effect not propagated).\n- Derived status labels (e.g. CrashLoopBackOff) render underlying quantities — restarts climbing, back-off events; the quantities are the evidence, the label their display form.\n- Fault effects propagate with physical delay (heartbeat grace, probe periods, image pull); an observation that predates propagation says nothing yet about the outcome.\n\n\n### Fault-Specific Verification Hints\nFault metadata: Scope: node | Target: disk | Action: burn\nFault scenario: node-disk burn\nFor 'disk' verification specifics (observation methods, partition/overlay or protocol semantics, data-interpretation pitfalls), load the relevant knowledge doc via `read_knowledge_resource` — e.g. `fault-verification-strategies.md`.\nEvidence sufficiency is your holistic judgment. A terminal/unreachable primary state can be conclusive on its own — do not probe into an unreachable target to collect an extra cross-metric (it only times out). If a cross-check helps, run it from a healthy peer.\n\n**Layer 1 Limitation**: Layer 1 only checks whether the fault experiment is registered. It does NOT verify that the fault effect is observable. Your Layer 2 verification is the ONLY way to confirm the fault is working.\n\n**NOTE**: Some minimal container images lack common shell utilities (top, ps, netstat, etc.). If a container exec check returns empty output or \"command not found\", do NOT retry — use a cluster API describe-style check instead.\n\n### Test Pod Namespace Selection\nWhen you need a Running application pod for verification tests (DNS resolution, network connectivity, service calls), do NOT only search the `default` namespace. Search in the TARGET namespace first (default), then other namespaces with workloads. For cluster-wide faults (DNS, node-level), any Running pod with shell access can serve as a test target.\n\n### Debug Pod Cleanup\nIf you create any pods during verification (e.g., temporary test pods), you MUST delete them before finishing verification. Add cleanup as a final step in your checklist.\nNote: framework-managed host-access pods (the carrier tool pods surfaced in the hints above) are DaemonSet-managed and MUST NOT be deleted.\n"
  },
  {
    "kind": "context",
    "label": "pod-disk-burn",
    "out": "## Layer 1 Result (already completed)\nLayer 1 for experiment uid-6: passed\nDetails: Success\n\n\n## Resource Coverage (from blade_query_k8s)\nAffected resources: 1\nTarget resources from context: ['test-node']\n\nCompare affected_count against the expected number of target resources. If fewer resources are affected than expected, the fault injection has INCOMPLETE COVERAGE. Investigate and report this in VERIFICATION_RESULT Warnings.\n## Fault Context\nSkill: pod-disk-burn\nTarget namespace: default\nTarget names: ['test-node']\nBlade params: {}\nKubeconfig: /kc\nScope: pod | Target: disk | Action: burn\n\n## Baseline Data Note\nBaseline capture was attempted but produced no usable data. You MUST be cautious when interpreting absolute metric values — high resource usage does NOT necessarily mean the fault is in effect. Cross-validate with multiple independent data points (metrics + events + conditions) before concluding 'verified'.\n\n## Injection Verification Instructions\nThe following skill use-case defines how to verify this fault. You MUST follow its verification approach as the primary reference.\n\n<skill-case>\n## 注入验证\n1. check dd\n\n</skill-case>\n\n### Verification Strategy (Template)\nSteps from the skill case:\n- Step 1: [status] — check dd\n\nStep handling:\n- Steps observing the injection itself taking effect on the target decide the verdict — they are your priority.\n- Steps describing propagated effects (OOM/eviction, latency, business impact) are NOT verdict criteria: if evidence is already in hand, record it faithfully (absence included); otherwise mark 'expected' or 'not_applicable' — NEVER add waiting, retries or extra sampling to observe them. If the step's target cannot be instantiated here (placeholder like '应用 A'), mark it 'not_applicable' with the reason; do NOT fabricate a target.\n\nRules:\n1. Line format: `Step N: <status> — <evidence>`,\n   keeping the case's step numbering.\n2. <status> ∈ passed, partial, failed, skipped, recovered_before_observation, expected, not_applicable.\n3. Every status needs evidence; for injection-effect steps `expected` without an observation is invalid — \n   use `skipped` if unchecked.\n4. ANSWER all 1 steps — any status with a\n   reason counts; silent omission is the only violation.\n5. Verdict: 'passed' requires ALL injection-effect steps\n   passed; a failed injection-effect step means the fault\n   may not be in effect ('failed'/'partial').\n6. Different method than specified? Note '(deviation: <why>)'.\n**Checklist Status Choice**:\n- The checklist reports OBSERVED FACTS, not predictions.\n- Steps observing the injection effect: did you perform the check? Yes → 'passed'/'failed' by what you OBSERVED; No → 'skipped'.\n- 'expected': the phenomenon is absent and the actual injection parameters make that the anticipated outcome. For propagated-effect steps (OOM, latency, business impact) this is valid WITHOUT observation; for injection-effect steps it requires the observation.\n- 'not_applicable': the step's target cannot exist in this environment (placeholder target, unnamed service). Do NOT fabricate a target.\n- Timing uncertainty belongs in Warnings, not in checklist status.\n- 'recovered_before_observation': the fault was transient and had dissipated by the time you checked — distinct from 'failed' (checked, fault absent).\n\nNote — Layer boundary: VERIFICATION_CHECKLIST must ONLY contain Layer 2 checks (observable fault effects). Do NOT include Layer 1 items (Layer 1 tool check, experiment registration).\n\nLayer 1 is PASSED. Now perform Layer 2 verification: use the available observation tools to verify the fault is actually in effect on the target.\n**NEGATIVE EVIDENCE ENUMERATION (CRITICAL)**:\nBefore concluding Layer2 'passed', you MUST include a 'Negative Evidence' section in your reasoning that explicitly lists EVERY observation contradicting or weakening the conclusion that the fault is in effect. For each item, either:\n(a) Dismiss it with factual basis (not speculation), or\n(b) Accept it as valid counter-evidence.\nIf ANY element of the effect claim (a significant change observed, attributable to the injection) is demonstrably NOT met, you MUST conclude Layer2 as 'partial' or 'failed' — NOT 'passed'. (Absence of propagated effects (OOM, latency, business impact) is NOT counter-evidence against the fault being in effect.)\n\n**EVIDENCE SEMANTICS (CRITICAL)**: what each observation means.\n- Qualitative faults (process gone / NotReady / unreachable): the observation is binary — a clear one settles the step.\n- Quantitative faults: the injected value (e.g. --mem-percent 80) is a mechanism parameter, not a measurement promise. The effect evidence is a significant change from baseline — or, with no baseline, significant deviation from the expected healthy state; a gap vs the injected value is magnitude information for Warnings.\n- Mechanism evidence (experiment registration, tool receipts, rule snapshots, unit liveness) speaks for the mechanism, not the outcome — field-proven: tools have reported Success while delivering no fault. When the effect is missing, it points to the failed layer (command not issued / mechanism not alive / payload spinning / effect not propagated).\n- Derived status labels (e.g. CrashLoopBackOff) render underlying quantities — restarts climbing, back-off events; the quantities are the evidence, the label their display form.\n- Fault effects propagate with physical delay (heartbeat grace, probe periods, image pull); an observation that predates propagation says nothing yet about the outcome.\n\n\n### Fault-Specific Verification Hints\nFault metadata: Scope: pod | Target: disk | Action: burn\nFault scenario: pod-disk burn\nFor 'disk' verification specifics (observation methods, partition/overlay or protocol semantics, data-interpretation pitfalls), load the relevant knowledge doc via `read_knowledge_resource` — e.g. `fault-verification-strategies.md`.\nEvidence sufficiency is your holistic judgment. A terminal/unreachable primary state can be conclusive on its own — do not probe into an unreachable target to collect an extra cross-metric (it only times out). If a cross-check helps, run it from a healthy peer.\n\n**Layer 1 Limitation**: Layer 1 only checks whether the fault experiment is registered. It does NOT verify that the fault effect is observable. Your Layer 2 verification is the ONLY way to confirm the fault is working.\n\n**NOTE**: Some minimal container images lack common shell utilities (top, ps, netstat, etc.). If a container exec check returns empty output or \"command not found\", do NOT retry — use a cluster API describe-style check instead.\n\n### Test Pod Namespace Selection\nWhen you need a Running application pod for verification tests (DNS resolution, network connectivity, service calls), do NOT only search the `default` namespace. Search in the TARGET namespace first (default), then other namespaces with workloads. For cluster-wide faults (DNS, node-level), any Running pod with shell access can serve as a test target.\n\n"
  },
  {
    "kind": "context",
    "label": "pod-mem-load",
    "out": "## Layer 1 Result (already completed)\nLayer 1 for experiment uid-7: passed\nDetails: Success\n\n\n## Resource Coverage (from blade_query_k8s)\nAffected resources: 1\nTarget resources from context: ['test-node']\n\nCompare affected_count against the expected number of target resources. If fewer resources are affected than expected, the fault injection has INCOMPLETE COVERAGE. Investigate and report this in VERIFICATION_RESULT Warnings.\n## Fault Context\nSkill: pod-mem-load\nTarget namespace: default\nTarget names: ['test-node']\nBlade params: {}\nKubeconfig: /kc\nInjection key parameters: {'percent': '80'}\nScope: pod | Target: mem | Action: load\n\n## Baseline Data Note\nBaseline capture was attempted but produced no usable data. You MUST be cautious when interpreting absolute metric values — high resource usage does NOT necessarily mean the fault is in effect. Cross-validate with multiple independent data points (metrics + events + conditions) before concluding 'verified'.\n\n## Injection Verification Instructions\nThe following skill use-case defines how to verify this fault. You MUST follow its verification approach as the primary reference.\n\n<skill-case>\n## 注入验证\n1. check mem\n\n</skill-case>\n\n### Verification Strategy (Template)\nSteps from the skill case:\n- Step 1: [status] — check mem\n\nStep handling:\n- Steps observing the injection itself taking effect on the target decide the verdict — they are your priority.\n- Steps describing propagated effects (OOM/eviction, latency, business impact) are NOT verdict criteria: if evidence is already in hand, record it faithfully (absence included); otherwise mark 'expected' or 'not_applicable' — NEVER add waiting, retries or extra sampling to observe them. If the step's target cannot be instantiated here (placeholder like '应用 A'), mark it 'not_applicable' with the reason; do NOT fabricate a target.\n\nRules:\n1. Line format: `Step N: <status> — <evidence>`,\n   keeping the case's step numbering.\n2. <status> ∈ passed, partial, failed, skipped, recovered_before_observation, expected, not_applicable.\n3. Every status needs evidence; for injection-effect steps `expected` without an observation is invalid — \n   use `skipped` if unchecked.\n4. ANSWER all 1 steps — any status with a\n   reason counts; silent omission is the only violation.\n5. Verdict: 'passed' requires ALL injection-effect steps\n   passed; a failed injection-effect step means the fault\n   may not be in effect ('failed'/'partial').\n6. Different method than specified? Note '(deviation: <why>)'.\n**Checklist Status Choice**:\n- The checklist reports OBSERVED FACTS, not predictions.\n- Steps observing the injection effect: did you perform the check? Yes → 'passed'/'failed' by what you OBSERVED; No → 'skipped'.\n- 'expected': the phenomenon is absent and the actual injection parameters make that the anticipated outcome. For propagated-effect steps (OOM, latency, business impact) this is valid WITHOUT observation; for injection-effect steps it requires the observation.\n- 'not_applicable': the step's target cannot exist in this environment (placeholder target, unnamed service). Do NOT fabricate a target.\n- Timing uncertainty belongs in Warnings, not in checklist status.\n- 'recovered_before_observation': the fault was transient and had dissipated by the time you checked — distinct from 'failed' (checked, fault absent).\n\nNote — Layer boundary: VERIFICATION_CHECKLIST must ONLY contain Layer 2 checks (observable fault effects). Do NOT include Layer 1 items (Layer 1 tool check, experiment registration).\n\nLayer 1 is PASSED. Now perform Layer 2 verification: use the available observation tools to verify the fault is actually in effect on the target.\n**NEGATIVE EVIDENCE ENUMERATION (CRITICAL)**:\nBefore concluding Layer2 'passed', you MUST include a 'Negative Evidence' section in your reasoning that explicitly lists EVERY observation contradicting or weakening the conclusion that the fault is in effect. For each item, either:\n(a) Dismiss it with factual basis (not speculation), or\n(b) Accept it as valid counter-evidence.\nIf ANY element of the effect claim (a significant change observed, attributable to the injection) is demonstrably NOT met, you MUST conclude Layer2 as 'partial' or 'failed' — NOT 'passed'. (Absence of propagated effects (OOM, latency, business impact) is NOT counter-evidence against the fault being in effect.)\n\n**EVIDENCE SEMANTICS (CRITICAL)**: what each observation means.\n- Qualitative faults (process gone / NotReady / unreachable): the observation is binary — a clear one settles the step.\n- Quantitative faults: the injected value (e.g. --mem-percent 80) is a mechanism parameter, not a measurement promise. The effect evidence is a significant change from baseline — or, with no baseline, significant deviation from the expected healthy state; a gap vs the injected value is magnitude information for Warnings.\n- Mechanism evidence (experiment registration, tool receipts, rule snapshots, unit liveness) speaks for the mechanism, not the outcome — field-proven: tools have reported Success while delivering no fault. When the effect is missing, it points to the failed layer (command not issued / mechanism not alive / payload spinning / effect not propagated).\n- Derived status labels (e.g. CrashLoopBackOff) render underlying quantities — restarts climbing, back-off events; the quantities are the evidence, the label their display form.\n- Fault effects propagate with physical delay (heartbeat grace, probe periods, image pull); an observation that predates propagation says nothing yet about the outcome.\n\n\n### Fault-Specific Verification Hints\nFault metadata: Scope: pod | Target: mem | Action: load\nFault scenario: pod-mem load\nFor 'mem' verification specifics (observation methods, partition/overlay or protocol semantics, data-interpretation pitfalls), load the relevant knowledge doc via `read_knowledge_resource` — e.g. `fault-verification-strategies.md`.\nEvidence sufficiency is your holistic judgment. A terminal/unreachable primary state can be conclusive on its own — do not probe into an unreachable target to collect an extra cross-metric (it only times out). If a cross-check helps, run it from a healthy peer.\n\n**Layer 1 Limitation**: Layer 1 only checks whether the fault experiment is registered. It does NOT verify that the fault effect is observable. Your Layer 2 verification is the ONLY way to confirm the fault is working.\n\n**NOTE**: Some minimal container images lack common shell utilities (top, ps, netstat, etc.). If a container exec check returns empty output or \"command not found\", do NOT retry — use a cluster API describe-style check instead.\n\n### Test Pod Namespace Selection\nWhen you need a Running application pod for verification tests (DNS resolution, network connectivity, service calls), do NOT only search the `default` namespace. Search in the TARGET namespace first (default), then other namespaces with workloads. For cluster-wide faults (DNS, node-level), any Running pod with shell access can serve as a test target.\n\n"
  },
  {
    "kind": "context",
    "label": "pod-process-kill",
    "out": "## Layer 1 Result (already completed)\nLayer 1 for experiment uid-8: passed\nDetails: Success\n\n\n## Resource Coverage (from blade_query_k8s)\nAffected resources: 1\nTarget resources from context: ['test-node']\n\nCompare affected_count against the expected number of target resources. If fewer resources are affected than expected, the fault injection has INCOMPLETE COVERAGE. Investigate and report this in VERIFICATION_RESULT Warnings.\n## Fault Context\nSkill: pod-process-kill\nTarget namespace: default\nTarget names: ['test-node']\nBlade params: {}\nKubeconfig: /kc\nScope: pod | Target: process | Action: kill\n\n## Baseline Data Note\nBaseline capture was attempted but produced no usable data. You MUST be cautious when interpreting absolute metric values — high resource usage does NOT necessarily mean the fault is in effect. Cross-validate with multiple independent data points (metrics + events + conditions) before concluding 'verified'.\n\n## Injection Verification Instructions\nThe following skill use-case defines how to verify this fault. You MUST follow its verification approach as the primary reference.\n\n<skill-case>\n## 注入验证\n1. check proc\n\n</skill-case>\n\n### Verification Strategy (Template)\nSteps from the skill case:\n- Step 1: [status] — check proc\n\nStep handling:\n- Steps observing the injection itself taking effect on the target decide the verdict — they are your priority.\n- Steps describing propagated effects (OOM/eviction, latency, business impact) are NOT verdict criteria: if evidence is already in hand, record it faithfully (absence included); otherwise mark 'expected' or 'not_applicable' — NEVER add waiting, retries or extra sampling to observe them. If the step's target cannot be instantiated here (placeholder like '应用 A'), mark it 'not_applicable' with the reason; do NOT fabricate a target.\n\nRules:\n1. Line format: `Step N: <status> — <evidence>`,\n   keeping the case's step numbering.\n2. <status> ∈ passed, partial, failed, skipped, recovered_before_observation, expected, not_applicable.\n3. Every status needs evidence; for injection-effect steps `expected` without an observation is invalid — \n   use `skipped` if unchecked.\n4. ANSWER all 1 steps — any status with a\n   reason counts; silent omission is the only violation.\n5. Verdict: 'passed' requires ALL injection-effect steps\n   passed; a failed injection-effect step means the fault\n   may not be in effect ('failed'/'partial').\n6. Different method than specified? Note '(deviation: <why>)'.\n**Checklist Status Choice**:\n- The checklist reports OBSERVED FACTS, not predictions.\n- Steps observing the injection effect: did you perform the check? Yes → 'passed'/'failed' by what you OBSERVED; No → 'skipped'.\n- 'expected': the phenomenon is absent and the actual injection parameters make that the anticipated outcome. For propagated-effect steps (OOM, latency, business impact) this is valid WITHOUT observation; for injection-effect steps it requires the observation.\n- 'not_applicable': the step's target cannot exist in this environment (placeholder target, unnamed service). Do NOT fabricate a target.\n- Timing uncertainty belongs in Warnings, not in checklist status.\n- 'recovered_before_observation': the fault was transient and had dissipated by the time you checked — distinct from 'failed' (checked, fault absent).\n\nNote — Layer boundary: VERIFICATION_CHECKLIST must ONLY contain Layer 2 checks (observable fault effects). Do NOT include Layer 1 items (Layer 1 tool check, experiment registration).\n\nLayer 1 is PASSED. Now perform Layer 2 verification: use the available observation tools to verify the fault is actually in effect on the target.\n**NEGATIVE EVIDENCE ENUMERATION (CRITICAL)**:\nBefore concluding Layer2 'passed', you MUST include a 'Negative Evidence' section in your reasoning that explicitly lists EVERY observation contradicting or weakening the conclusion that the fault is in effect. For each item, either:\n(a) Dismiss it with factual basis (not speculation), or\n(b) Accept it as valid counter-evidence.\nIf ANY element of the effect claim (a significant change observed, attributable to the injection) is demonstrably NOT met, you MUST conclude Layer2 as 'partial' or 'failed' — NOT 'passed'. (Absence of propagated effects (OOM, latency, business impact) is NOT counter-evidence against the fault being in effect.)\n\n**EVIDENCE SEMANTICS (CRITICAL)**: what each observation means.\n- Qualitative faults (process gone / NotReady / unreachable): the observation is binary — a clear one settles the step.\n- Quantitative faults: the injected value (e.g. --mem-percent 80) is a mechanism parameter, not a measurement promise. The effect evidence is a significant change from baseline — or, with no baseline, significant deviation from the expected healthy state; a gap vs the injected value is magnitude information for Warnings.\n- Mechanism evidence (experiment registration, tool receipts, rule snapshots, unit liveness) speaks for the mechanism, not the outcome — field-proven: tools have reported Success while delivering no fault. When the effect is missing, it points to the failed layer (command not issued / mechanism not alive / payload spinning / effect not propagated).\n- Derived status labels (e.g. CrashLoopBackOff) render underlying quantities — restarts climbing, back-off events; the quantities are the evidence, the label their display form.\n- Fault effects propagate with physical delay (heartbeat grace, probe periods, image pull); an observation that predates propagation says nothing yet about the outcome.\n\n\n### Fault-Specific Verification Hints\nFault metadata: Scope: pod | Target: process | Action: kill\nFault scenario: pod-process kill\nFor 'process' verification specifics (observation methods, partition/overlay or protocol semantics, data-interpretation pitfalls), load the relevant knowledge doc via `read_knowledge_resource` — e.g. `fault-verification-strategies.md`.\nEvidence sufficiency is your holistic judgment. A terminal/unreachable primary state can be conclusive on its own — do not probe into an unreachable target to collect an extra cross-metric (it only times out). If a cross-check helps, run it from a healthy peer.\n\n**Layer 1 Limitation**: Layer 1 only checks whether the fault experiment is registered. It does NOT verify that the fault effect is observable. Your Layer 2 verification is the ONLY way to confirm the fault is working.\n\n**NOTE**: Some minimal container images lack common shell utilities (top, ps, netstat, etc.). If a container exec check returns empty output or \"command not found\", do NOT retry — use a cluster API describe-style check instead.\n\n### Test Pod Namespace Selection\nWhen you need a Running application pod for verification tests (DNS resolution, network connectivity, service calls), do NOT only search the `default` namespace. Search in the TARGET namespace first (default), then other namespaces with workloads. For cluster-wide faults (DNS, node-level), any Running pod with shell access can serve as a test target.\n\n"
  },
  {
    "kind": "context",
    "label": "pod-network-dns",
    "out": "## Layer 1 Result (already completed)\nLayer 1 for experiment uid-9: passed\nDetails: Success\n\n\n## Resource Coverage (from blade_query_k8s)\nAffected resources: 1\nTarget resources from context: ['test-node']\n\nCompare affected_count against the expected number of target resources. If fewer resources are affected than expected, the fault injection has INCOMPLETE COVERAGE. Investigate and report this in VERIFICATION_RESULT Warnings.\n## Fault Context\nSkill: pod-network-dns\nTarget namespace: default\nTarget names: ['test-node']\nBlade params: {}\nKubeconfig: /kc\nScope: pod | Target: network | Action: dns\n\n## Baseline Data Note\nBaseline capture was attempted but produced no usable data. You MUST be cautious when interpreting absolute metric values — high resource usage does NOT necessarily mean the fault is in effect. Cross-validate with multiple independent data points (metrics + events + conditions) before concluding 'verified'.\n\n## Injection Verification Instructions\nNo skill use-case content is available. Design verification based on the fault-specific hints below and your expertise.\nBefore your final conclusion, output a **Verification Checklist** listing each check and its result:\n   - Check 1: passed/failed/skipped — brief evidence\n   - ...\n**WARNING**: Without skill guidance, verification may be incomplete. At minimum, verify the fault effect is observable on the target.\n**Knowledge docs**: Check the Domain Knowledge Index for documents whose \"When to read\" field covers your current scenario (e.g., verification strategies, kubectl field reference). Use `read_knowledge_resource` to load them before designing your verification plan.\n\nLayer 1 is PASSED. Now perform Layer 2 verification: use the available observation tools to verify the fault is actually in effect on the target.\n**NEGATIVE EVIDENCE ENUMERATION (CRITICAL)**:\nBefore concluding Layer2 'passed', you MUST include a 'Negative Evidence' section in your reasoning that explicitly lists EVERY observation contradicting or weakening the conclusion that the fault is in effect. For each item, either:\n(a) Dismiss it with factual basis (not speculation), or\n(b) Accept it as valid counter-evidence.\nIf ANY element of the effect claim (a significant change observed, attributable to the injection) is demonstrably NOT met, you MUST conclude Layer2 as 'partial' or 'failed' — NOT 'passed'. (Absence of propagated effects (OOM, latency, business impact) is NOT counter-evidence against the fault being in effect.)\n\n**EVIDENCE SEMANTICS (CRITICAL)**: what each observation means.\n- Qualitative faults (process gone / NotReady / unreachable): the observation is binary — a clear one settles the step.\n- Quantitative faults: the injected value (e.g. --mem-percent 80) is a mechanism parameter, not a measurement promise. The effect evidence is a significant change from baseline — or, with no baseline, significant deviation from the expected healthy state; a gap vs the injected value is magnitude information for Warnings.\n- Mechanism evidence (experiment registration, tool receipts, rule snapshots, unit liveness) speaks for the mechanism, not the outcome — field-proven: tools have reported Success while delivering no fault. When the effect is missing, it points to the failed layer (command not issued / mechanism not alive / payload spinning / effect not propagated).\n- Derived status labels (e.g. CrashLoopBackOff) render underlying quantities — restarts climbing, back-off events; the quantities are the evidence, the label their display form.\n- Fault effects propagate with physical delay (heartbeat grace, probe periods, image pull); an observation that predates propagation says nothing yet about the outcome.\n\n\n### Fault-Specific Verification Hints\nFault metadata: Scope: pod | Target: network | Action: dns\nFault scenario: pod-network dns\nFor 'network' verification specifics (observation methods, partition/overlay or protocol semantics, data-interpretation pitfalls), load the relevant knowledge doc via `read_knowledge_resource` — e.g. `fault-verification-strategies.md`.\nEvidence sufficiency is your holistic judgment. A terminal/unreachable primary state can be conclusive on its own — do not probe into an unreachable target to collect an extra cross-metric (it only times out). If a cross-check helps, run it from a healthy peer.\n\n**Layer 1 Limitation**: Layer 1 only checks whether the fault experiment is registered. It does NOT verify that the fault effect is observable. Your Layer 2 verification is the ONLY way to confirm the fault is working.\n\n**NOTE**: Some minimal container images lack common shell utilities (top, ps, netstat, etc.). If a container exec check returns empty output or \"command not found\", do NOT retry — use a cluster API describe-style check instead.\n\n### Test Pod Namespace Selection\nWhen you need a Running application pod for verification tests (DNS resolution, network connectivity, service calls), do NOT only search the `default` namespace. Search in the TARGET namespace first (default), then other namespaces with workloads. For cluster-wide faults (DNS, node-level), any Running pod with shell access can serve as a test target.\n\n"
  },
  {
    "kind": "context",
    "label": "pod-cpu-load",
    "out": "## Layer 1 Result (already completed)\nLayer 1 for experiment uid-10: passed\nDetails: Success\n\n\n## Resource Coverage (from blade_query_k8s)\nAffected resources: 1\nTarget resources from context: ['test-node']\n\nCompare affected_count against the expected number of target resources. If fewer resources are affected than expected, the fault injection has INCOMPLETE COVERAGE. Investigate and report this in VERIFICATION_RESULT Warnings.\n## Fault Context\nSkill: pod-cpu-load\nTarget namespace: default\nTarget names: ['test-node']\nBlade params: {}\nKubeconfig: /kc\nInjection key parameters: {'percent': '90'}\nScope: pod | Target: cpu | Action: load\n\n## Baseline Data Note\nBaseline capture was attempted but produced no usable data. You MUST be cautious when interpreting absolute metric values — high resource usage does NOT necessarily mean the fault is in effect. Cross-validate with multiple independent data points (metrics + events + conditions) before concluding 'verified'.\n\n## Injection Verification Instructions\nNo skill use-case content is available. Design verification based on the fault-specific hints below and your expertise.\nBefore your final conclusion, output a **Verification Checklist** listing each check and its result:\n   - Check 1: passed/failed/skipped — brief evidence\n   - ...\n**WARNING**: Without skill guidance, verification may be incomplete. At minimum, verify the fault effect is observable on the target.\n**Knowledge docs**: Check the Domain Knowledge Index for documents whose \"When to read\" field covers your current scenario (e.g., verification strategies, kubectl field reference). Use `read_knowledge_resource` to load them before designing your verification plan.\n\nLayer 1 is PASSED. Now perform Layer 2 verification: use the available observation tools to verify the fault is actually in effect on the target.\n**NEGATIVE EVIDENCE ENUMERATION (CRITICAL)**:\nBefore concluding Layer2 'passed', you MUST include a 'Negative Evidence' section in your reasoning that explicitly lists EVERY observation contradicting or weakening the conclusion that the fault is in effect. For each item, either:\n(a) Dismiss it with factual basis (not speculation), or\n(b) Accept it as valid counter-evidence.\nIf ANY element of the effect claim (a significant change observed, attributable to the injection) is demonstrably NOT met, you MUST conclude Layer2 as 'partial' or 'failed' — NOT 'passed'. (Absence of propagated effects (OOM, latency, business impact) is NOT counter-evidence against the fault being in effect.)\n\n**EVIDENCE SEMANTICS (CRITICAL)**: what each observation means.\n- Qualitative faults (process gone / NotReady / unreachable): the observation is binary — a clear one settles the step.\n- Quantitative faults: the injected value (e.g. --mem-percent 80) is a mechanism parameter, not a measurement promise. The effect evidence is a significant change from baseline — or, with no baseline, significant deviation from the expected healthy state; a gap vs the injected value is magnitude information for Warnings.\n- Mechanism evidence (experiment registration, tool receipts, rule snapshots, unit liveness) speaks for the mechanism, not the outcome — field-proven: tools have reported Success while delivering no fault. When the effect is missing, it points to the failed layer (command not issued / mechanism not alive / payload spinning / effect not propagated).\n- Derived status labels (e.g. CrashLoopBackOff) render underlying quantities — restarts climbing, back-off events; the quantities are the evidence, the label their display form.\n- Fault effects propagate with physical delay (heartbeat grace, probe periods, image pull); an observation that predates propagation says nothing yet about the outcome.\n\n\n### Fault-Specific Verification Hints\nFault metadata: Scope: pod | Target: cpu | Action: load\nFault scenario: pod-cpu load\nFor 'cpu' verification specifics (observation methods, partition/overlay or protocol semantics, data-interpretation pitfalls), load the relevant knowledge doc via `read_knowledge_resource` — e.g. `fault-verification-strategies.md`.\nEvidence sufficiency is your holistic judgment. A terminal/unreachable primary state can be conclusive on its own — do not probe into an unreachable target to collect an extra cross-metric (it only times out). If a cross-check helps, run it from a healthy peer.\n\n**Layer 1 Limitation**: Layer 1 only checks whether the fault experiment is registered. It does NOT verify that the fault effect is observable. Your Layer 2 verification is the ONLY way to confirm the fault is working.\n\n**NOTE**: Some minimal container images lack common shell utilities (top, ps, netstat, etc.). If a container exec check returns empty output or \"command not found\", do NOT retry — use a cluster API describe-style check instead.\n\n### Test Pod Namespace Selection\nWhen you need a Running application pod for verification tests (DNS resolution, network connectivity, service calls), do NOT only search the `default` namespace. Search in the TARGET namespace first (default), then other namespaces with workloads. For cluster-wide faults (DNS, node-level), any Running pod with shell access can serve as a test target.\n\n"
  },
  {
    "kind": "context",
    "label": "node-disk-fill-selfdestruct-skipped",
    "out": "## Layer 1 Result (SKIPPED — self-destructive fault)\nLayer 1 tool check unreachable: node is self-destructive and NotReady\n\n## WARNING: Target node is NotReady\nThe target node lost connectivity — this is likely the injection effect itself (e.g. containerd/kubelet stopped). Your verification MUST use commands that go through the API server (not the node):\n- `kubectl get nodes` — confirm node NotReady\n- `kubectl get pods` — check for ContainerCreating / Terminating\n- `kubectl describe pod` — check Events for runtime errors\n- `kubectl get events` — check for node-level events\nDo NOT use `kubectl exec` into pods on the affected node (it will fail because the node is unreachable).\n\n\n## Resource Coverage (from blade_query_k8s)\nAffected resources: 1\nTarget resources from context: ['test-node']\n\nCompare affected_count against the expected number of target resources. If fewer resources are affected than expected, the fault injection has INCOMPLETE COVERAGE. Investigate and report this in VERIFICATION_RESULT Warnings.\n## Fault Context\nSkill: node-disk-fill\nTarget namespace: default\nTarget names: ['test-node']\nBlade params: {}\nKubeconfig: /kc\nInjection key parameters: {'path': '/tmp', 'size': '3000'}\nScope: node | Target: disk | Action: fill\n\n## Baseline Data Note\nBaseline capture was attempted but produced no usable data. You MUST be cautious when interpreting absolute metric values — high resource usage does NOT necessarily mean the fault is in effect. Cross-validate with multiple independent data points (metrics + events + conditions) before concluding 'verified'.\n\n## Available Tool Pod\nA tool pod is available for cluster-level operations:\n- Pod name: `tool-pod-k`\n- Namespace: unknown — identify it across all namespaces before exec (the namespace is deployment-specific; never assume it)\n- Capabilities: injection-tool commands (status/destroy), cluster API checks (describe/top/get), and host-level checks via `/host/...` (the tool pod typically mounts the host root).\n- For CRD-mode disk fill, the fill file IS in the container overlay — checking it inside this pod is the PRIMARY verification method.\n- For host filesystem verification (e.g., `/proc/loadavg`, `/var/log`), prefix paths with `/host` (try `/host/proc/loadavg` first; if missing, fall back to bare `/proc/loadavg`). If this tool pod is unavailable or lacks /host access, fall back to a node debug pod (busybox image) and exec into it.\n- **UID Dual Mapping**: The experiment UID (uid-11) is the CRD resource name. Inside the tool pod, the injection tool's local status subcommand searches the LOCAL experiment database and typically returns 'record not found' for an experiment created through the cluster API — NEVER use it for this check (it causes a false-negative Layer 2 conclusion). Query the experiment CRD through the cluster API instead (discover the experiment resource kind via the cluster query tool itself; knowledge docs provide reference forms).\n\n## Injection Verification Instructions\nNo skill use-case content is available. Design verification based on the fault-specific hints below and your expertise.\nBefore your final conclusion, output a **Verification Checklist** listing each check and its result:\n   - Check 1: passed/failed/skipped — brief evidence\n   - ...\n**WARNING**: Without skill guidance, verification may be incomplete. At minimum, verify the fault effect is observable on the target.\n**Knowledge docs**: Check the Domain Knowledge Index for documents whose \"When to read\" field covers your current scenario (e.g., verification strategies, kubectl field reference). Use `read_knowledge_resource` to load them before designing your verification plan.\n\nThis is a self-destructive fault: the injection destroyed the node's communication channel. Verify the EFFECT of the fault (node NotReady, pods in abnormal state) rather than the injection mechanism (Layer 1 tool check).\n**NEGATIVE EVIDENCE ENUMERATION (CRITICAL)**:\nBefore concluding Layer2 'passed', you MUST include a 'Negative Evidence' section in your reasoning that explicitly lists EVERY observation contradicting or weakening the conclusion that the fault is in effect. For each item, either:\n(a) Dismiss it with factual basis (not speculation), or\n(b) Accept it as valid counter-evidence.\nIf ANY element of the effect claim (a significant change observed, attributable to the injection) is demonstrably NOT met, you MUST conclude Layer2 as 'partial' or 'failed' — NOT 'passed'. (Absence of propagated effects (OOM, latency, business impact) is NOT counter-evidence against the fault being in effect.)\n\n**EVIDENCE SEMANTICS (CRITICAL)**: what each observation means.\n- Qualitative faults (process gone / NotReady / unreachable): the observation is binary — a clear one settles the step.\n- Quantitative faults: the injected value (e.g. --mem-percent 80) is a mechanism parameter, not a measurement promise. The effect evidence is a significant change from baseline — or, with no baseline, significant deviation from the expected healthy state; a gap vs the injected value is magnitude information for Warnings.\n- Mechanism evidence (experiment registration, tool receipts, rule snapshots, unit liveness) speaks for the mechanism, not the outcome — field-proven: tools have reported Success while delivering no fault. When the effect is missing, it points to the failed layer (command not issued / mechanism not alive / payload spinning / effect not propagated).\n- Derived status labels (e.g. CrashLoopBackOff) render underlying quantities — restarts climbing, back-off events; the quantities are the evidence, the label their display form.\n- Fault effects propagate with physical delay (heartbeat grace, probe periods, image pull); an observation that predates propagation says nothing yet about the outcome.\n\n\n### Fault-Specific Verification Hints\nFault metadata: Scope: node | Target: disk | Action: fill\nFault scenario: node-disk fill\nFor 'disk' verification specifics (observation methods, partition/overlay or protocol semantics, data-interpretation pitfalls), load the relevant knowledge doc via `read_knowledge_resource` — e.g. `fault-verification-strategies.md`.\nEvidence sufficiency is your holistic judgment. A terminal/unreachable primary state can be conclusive on its own — do not probe into an unreachable target to collect an extra cross-metric (it only times out). If a cross-check helps, run it from a healthy peer.\n\n**Layer 1 Limitation**: Layer 1 only checks whether the fault experiment is registered. It does NOT verify that the fault effect is observable. Your Layer 2 verification is the ONLY way to confirm the fault is working.\n\n**NOTE**: Some minimal container images lack common shell utilities (top, ps, netstat, etc.). If a container exec check returns empty output or \"command not found\", do NOT retry — use a cluster API describe-style check instead.\n\n### Test Pod Namespace Selection\nWhen you need a Running application pod for verification tests (DNS resolution, network connectivity, service calls), do NOT only search the `default` namespace. Search in the TARGET namespace first (default), then other namespaces with workloads. For cluster-wide faults (DNS, node-level), any Running pod with shell access can serve as a test target.\n\n### Debug Pod Cleanup\nIf you create any pods during verification (e.g., temporary test pods), you MUST delete them before finishing verification. Add cleanup as a final step in your checklist.\nNote: framework-managed host-access pods (the carrier tool pods surfaced in the hints above) are DaemonSet-managed and MUST NOT be deleted.\n"
  },
  {
    "kind": "context",
    "label": "node-disk-fill-postcheck-found",
    "out": "## Layer 1 Result (already completed)\nLayer 1 for experiment uid-12: passed\nDetails: Success\n\n\n## Resource Coverage (from blade_query_k8s)\nAffected resources: 1\nTarget resources from context: ['test-node']\n\nCompare affected_count against the expected number of target resources. If fewer resources are affected than expected, the fault injection has INCOMPLETE COVERAGE. Investigate and report this in VERIFICATION_RESULT Warnings.\n## Fault Context\nSkill: node-disk-fill\nTarget namespace: default\nTarget names: ['test-node']\nBlade params: {}\nKubeconfig: /kc\nInjection key parameters: {'path': '/var/lib/docker', 'size': '8000'}\nScope: node | Target: disk | Action: fill\n\n## Baseline Data Note\nBaseline capture was attempted but produced no usable data. You MUST be cautious when interpreting absolute metric values — high resource usage does NOT necessarily mean the fault is in effect. Cross-validate with multiple independent data points (metrics + events + conditions) before concluding 'verified'.\n\n## Available Tool Pod\nA tool pod is available for cluster-level operations:\n- Pod name: `tool-pod-l`\n- Namespace: unknown — identify it across all namespaces before exec (the namespace is deployment-specific; never assume it)\n- Capabilities: injection-tool commands (status/destroy), cluster API checks (describe/top/get), and host-level checks via `/host/...` (the tool pod typically mounts the host root).\n- For CRD-mode disk fill, the fill file IS in the container overlay — checking it inside this pod is the PRIMARY verification method.\n- For host filesystem verification (e.g., `/proc/loadavg`, `/var/log`), prefix paths with `/host` (try `/host/proc/loadavg` first; if missing, fall back to bare `/proc/loadavg`). If this tool pod is unavailable or lacks /host access, fall back to a node debug pod (busybox image) and exec into it.\n- **UID Dual Mapping**: The experiment UID (uid-12) is the CRD resource name. Inside the tool pod, the injection tool's local status subcommand searches the LOCAL experiment database and typically returns 'record not found' for an experiment created through the cluster API — NEVER use it for this check (it causes a false-negative Layer 2 conclusion). Query the experiment CRD through the cluster API instead (discover the experiment resource kind via the cluster query tool itself; knowledge docs provide reference forms).\n\n## Injection Engine Post-Check (already executed)\nThe injection engine programmatically verified the fill effect on target node via tool pod `tp`:\n- **Fill file FOUND** in container overlay — injection is WORKING.\n- `ls` output:\n```\nchaos_filldisk.log.dat\n```\n- `df -h` output:\n```\nvdb 90%\n```\nUse this as PRIMARY evidence. If fill file was found, you have direct proof the fault is in effect. If not found, the fault may not have worked — cross-validate with other checks.\n\n## Injection Verification Instructions\nNo skill use-case content is available. Design verification based on the fault-specific hints below and your expertise.\nBefore your final conclusion, output a **Verification Checklist** listing each check and its result:\n   - Check 1: passed/failed/skipped — brief evidence\n   - ...\n**WARNING**: Without skill guidance, verification may be incomplete. At minimum, verify the fault effect is observable on the target.\n**Knowledge docs**: Check the Domain Knowledge Index for documents whose \"When to read\" field covers your current scenario (e.g., verification strategies, kubectl field reference). Use `read_knowledge_resource` to load them before designing your verification plan.\n\nLayer 1 is PASSED. Now perform Layer 2 verification: use the available observation tools to verify the fault is actually in effect on the target.\n**NEGATIVE EVIDENCE ENUMERATION (CRITICAL)**:\nBefore concluding Layer2 'passed', you MUST include a 'Negative Evidence' section in your reasoning that explicitly lists EVERY observation contradicting or weakening the conclusion that the fault is in effect. For each item, either:\n(a) Dismiss it with factual basis (not speculation), or\n(b) Accept it as valid counter-evidence.\nIf ANY element of the effect claim (a significant change observed, attributable to the injection) is demonstrably NOT met, you MUST conclude Layer2 as 'partial' or 'failed' — NOT 'passed'. (Absence of propagated effects (OOM, latency, business impact) is NOT counter-evidence against the fault being in effect.)\n\n**EVIDENCE SEMANTICS (CRITICAL)**: what each observation means.\n- Qualitative faults (process gone / NotReady / unreachable): the observation is binary — a clear one settles the step.\n- Quantitative faults: the injected value (e.g. --mem-percent 80) is a mechanism parameter, not a measurement promise. The effect evidence is a significant change from baseline — or, with no baseline, significant deviation from the expected healthy state; a gap vs the injected value is magnitude information for Warnings.\n- Mechanism evidence (experiment registration, tool receipts, rule snapshots, unit liveness) speaks for the mechanism, not the outcome — field-proven: tools have reported Success while delivering no fault. When the effect is missing, it points to the failed layer (command not issued / mechanism not alive / payload spinning / effect not propagated).\n- Derived status labels (e.g. CrashLoopBackOff) render underlying quantities — restarts climbing, back-off events; the quantities are the evidence, the label their display form.\n- Fault effects propagate with physical delay (heartbeat grace, probe periods, image pull); an observation that predates propagation says nothing yet about the outcome.\n\n\n### Fault-Specific Verification Hints\nFault metadata: Scope: node | Target: disk | Action: fill\nFault scenario: node-disk fill\nFor 'disk' verification specifics (observation methods, partition/overlay or protocol semantics, data-interpretation pitfalls), load the relevant knowledge doc via `read_knowledge_resource` — e.g. `fault-verification-strategies.md`.\nEvidence sufficiency is your holistic judgment. A terminal/unreachable primary state can be conclusive on its own — do not probe into an unreachable target to collect an extra cross-metric (it only times out). If a cross-check helps, run it from a healthy peer.\n\n**Layer 1 Limitation**: Layer 1 only checks whether the fault experiment is registered. It does NOT verify that the fault effect is observable. Your Layer 2 verification is the ONLY way to confirm the fault is working.\n\n**NOTE**: Some minimal container images lack common shell utilities (top, ps, netstat, etc.). If a container exec check returns empty output or \"command not found\", do NOT retry — use a cluster API describe-style check instead.\n\n### Test Pod Namespace Selection\nWhen you need a Running application pod for verification tests (DNS resolution, network connectivity, service calls), do NOT only search the `default` namespace. Search in the TARGET namespace first (default), then other namespaces with workloads. For cluster-wide faults (DNS, node-level), any Running pod with shell access can serve as a test target.\n\n### Debug Pod Cleanup\nIf you create any pods during verification (e.g., temporary test pods), you MUST delete them before finishing verification. Add cleanup as a final step in your checklist.\nNote: framework-managed host-access pods (the carrier tool pods surfaced in the hints above) are DaemonSet-managed and MUST NOT be deleted.\n"
  },
  {
    "kind": "baseline",
    "label": "bl-disk-fill-imagefs",
    "out": [
      {
        "type": "AIMessage",
        "content": ""
      },
      {
        "type": "ToolMessage",
        "content": "Pre-injection baseline collected at 2026-05-09T10:00:00 (strategy: registry, 1/1 succeeded).\n\nThese metrics were captured BEFORE fault injection using the same kubectl commands you would use for verification. This is your authoritative reference — compare every post-injection observation against these values using delta format: \"baseline: X → current: Y (ΔZ)\".\n\n### df -h\nCommand: `kubectl exec ... df -h`\n```\nFilesystem Size Used Avail Use% Mounted on\n/dev/vda3 40G 6G 34G 16% /host\n/dev/vdb 20G 8G 12G 42% /\n```"
      },
      {
        "type": "AIMessage",
        "content": ""
      },
      {
        "type": "ToolMessage",
        "content": "### Baseline Key Metrics (extracted — compare against these)\n- Disk usage (nodefs): 16% (6G/40G)\n- Disk usage (overlay): 42% (8G/20G)\n\n### Baseline Comparison Rules\nYou now have PRE-INJECTION baseline data (captured BEFORE the fault was injected). This is MORE RELIABLE than your first post-injection observation because the baseline values are guaranteed to be unaffected by the fault.\nRules:\n- Compare post-injection metrics against the pre-injection baseline above, NOT against your first post-injection check.\n- A significant change from baseline (e.g., disk 10%→13%, CPU 100m→800m, RestartCount 7→8) is STRONG evidence the fault is in effect.\n- If metrics are SIMILAR to baseline, the fault may not be working.\n\n**FORMAT REQUIREMENT (when Pre-Injection Baseline is available)**:\nFor steps that decide whether the injection took effect, include baseline comparison in the evidence in the format:\n  \"baseline: <metric from above> → post-injection: <metric you observe NOW> (Δ<change>)\"\nSteps without a baseline comparison weaken their own evidence. Propagated-effect steps (OOM, latency, business impact) may omit it — mark them 'expected'/'not_applicable' when no observation is in hand.\nIn your VERIFICATION_RESULT, set BaselineUsed: true.\n"
      }
    ]
  },
  {
    "kind": "baseline",
    "label": "bl-disk-fill-nodefs",
    "out": [
      {
        "type": "AIMessage",
        "content": ""
      },
      {
        "type": "ToolMessage",
        "content": "Pre-injection baseline collected at 2026-05-09T10:00:00 (strategy: registry, 1/1 succeeded).\n\nThese metrics were captured BEFORE fault injection using the same kubectl commands you would use for verification. This is your authoritative reference — compare every post-injection observation against these values using delta format: \"baseline: X → current: Y (ΔZ)\".\n\n### df -h\nCommand: `kubectl exec ... df -h`\n```\nFilesystem Size Used Avail Use% Mounted on\n/dev/vda3 40G 6G 34G 16% /host\n/dev/vdb 20G 8G 12G 42% /\n```"
      },
      {
        "type": "AIMessage",
        "content": ""
      },
      {
        "type": "ToolMessage",
        "content": "### Baseline Key Metrics (extracted — compare against these)\n- Disk usage (nodefs): 16% (6G/40G)\n- Disk usage (overlay): 42% (8G/20G)\n\n### Baseline Comparison Rules\nYou now have PRE-INJECTION baseline data (captured BEFORE the fault was injected). This is MORE RELIABLE than your first post-injection observation because the baseline values are guaranteed to be unaffected by the fault.\nRules:\n- Compare post-injection metrics against the pre-injection baseline above, NOT against your first post-injection check.\n- A significant change from baseline (e.g., disk 10%→13%, CPU 100m→800m, RestartCount 7→8) is STRONG evidence the fault is in effect.\n- If metrics are SIMILAR to baseline, the fault may not be working.\n\n**FORMAT REQUIREMENT (when Pre-Injection Baseline is available)**:\nFor steps that decide whether the injection took effect, include baseline comparison in the evidence in the format:\n  \"baseline: <metric from above> → post-injection: <metric you observe NOW> (Δ<change>)\"\nSteps without a baseline comparison weaken their own evidence. Propagated-effect steps (OOM, latency, business impact) may omit it — mark them 'expected'/'not_applicable' when no observation is in hand.\nIn your VERIFICATION_RESULT, set BaselineUsed: true.\n"
      }
    ]
  },
  {
    "kind": "baseline",
    "label": "bl-disk-fill-unknown",
    "out": [
      {
        "type": "AIMessage",
        "content": ""
      },
      {
        "type": "ToolMessage",
        "content": "Pre-injection baseline collected at 2026-05-09T10:00:00 (strategy: registry, 1/1 succeeded).\n\nThese metrics were captured BEFORE fault injection using the same kubectl commands you would use for verification. This is your authoritative reference — compare every post-injection observation against these values using delta format: \"baseline: X → current: Y (ΔZ)\".\n\n### df -h\nCommand: `kubectl exec ... df -h`\n```\nFilesystem Size Used Avail Use% Mounted on\n/dev/vda3 40G 6G 34G 16% /host\n/dev/vdb 20G 8G 12G 42% /\n```"
      },
      {
        "type": "AIMessage",
        "content": ""
      },
      {
        "type": "ToolMessage",
        "content": "### Baseline Key Metrics (extracted — compare against these)\n- Disk usage (nodefs): 16% (6G/40G)\n- Disk usage (overlay): 42% (8G/20G)\n\n### Baseline Comparison Rules\nYou now have PRE-INJECTION baseline data (captured BEFORE the fault was injected). This is MORE RELIABLE than your first post-injection observation because the baseline values are guaranteed to be unaffected by the fault.\nRules:\n- Compare post-injection metrics against the pre-injection baseline above, NOT against your first post-injection check.\n- A significant change from baseline (e.g., disk 10%→13%, CPU 100m→800m, RestartCount 7→8) is STRONG evidence the fault is in effect.\n- If metrics are SIMILAR to baseline, the fault may not be working.\n\n**FORMAT REQUIREMENT (when Pre-Injection Baseline is available)**:\nFor steps that decide whether the injection took effect, include baseline comparison in the evidence in the format:\n  \"baseline: <metric from above> → post-injection: <metric you observe NOW> (Δ<change>)\"\nSteps without a baseline comparison weaken their own evidence. Propagated-effect steps (OOM, latency, business impact) may omit it — mark them 'expected'/'not_applicable' when no observation is in hand.\nIn your VERIFICATION_RESULT, set BaselineUsed: true.\n"
      }
    ]
  },
  {
    "kind": "baseline",
    "label": "bl-disk-burn",
    "out": [
      {
        "type": "AIMessage",
        "content": ""
      },
      {
        "type": "ToolMessage",
        "content": "Pre-injection baseline collected at 2026-05-09T10:00:00 (strategy: registry, 1/1 succeeded).\n\nThese metrics were captured BEFORE fault injection using the same kubectl commands you would use for verification. This is your authoritative reference — compare every post-injection observation against these values using delta format: \"baseline: X → current: Y (ΔZ)\".\n\n### df -h\nCommand: `kubectl exec ... df -h`\n```\nFilesystem Size Used Avail Use% Mounted on\n/dev/vda3 40G 6G 34G 16% /host\n/dev/vdb 20G 8G 12G 42% /\n```"
      },
      {
        "type": "AIMessage",
        "content": ""
      },
      {
        "type": "ToolMessage",
        "content": "### Baseline Key Metrics (extracted — compare against these)\n- Disk usage (nodefs): 16% (6G/40G)\n- Disk usage (overlay): 42% (8G/20G)\n\n### Baseline Comparison Rules\nYou now have PRE-INJECTION baseline data (captured BEFORE the fault was injected). This is MORE RELIABLE than your first post-injection observation because the baseline values are guaranteed to be unaffected by the fault.\nRules:\n- Compare post-injection metrics against the pre-injection baseline above, NOT against your first post-injection check.\n- A significant change from baseline (e.g., disk 10%→13%, CPU 100m→800m, RestartCount 7→8) is STRONG evidence the fault is in effect.\n- If metrics are SIMILAR to baseline, the fault may not be working.\n\n**FORMAT REQUIREMENT (when Pre-Injection Baseline is available)**:\nFor steps that decide whether the injection took effect, include baseline comparison in the evidence in the format:\n  \"baseline: <metric from above> → post-injection: <metric you observe NOW> (Δ<change>)\"\nSteps without a baseline comparison weaken their own evidence. Propagated-effect steps (OOM, latency, business impact) may omit it — mark them 'expected'/'not_applicable' when no observation is in hand.\nIn your VERIFICATION_RESULT, set BaselineUsed: true.\n"
      }
    ]
  },
  {
    "kind": "baseline",
    "label": "bl-mem",
    "out": [
      {
        "type": "AIMessage",
        "content": ""
      },
      {
        "type": "ToolMessage",
        "content": "Pre-injection baseline collected at 2026-05-09T10:00:00 (strategy: registry, 1/1 succeeded).\n\nThese metrics were captured BEFORE fault injection using the same kubectl commands you would use for verification. This is your authoritative reference — compare every post-injection observation against these values using delta format: \"baseline: X → current: Y (ΔZ)\".\n\n### df -h\nCommand: `kubectl exec ... df -h`\n```\nFilesystem Size Used Avail Use% Mounted on\n/dev/vda3 40G 6G 34G 16% /host\n/dev/vdb 20G 8G 12G 42% /\n```"
      },
      {
        "type": "AIMessage",
        "content": ""
      },
      {
        "type": "ToolMessage",
        "content": "### Baseline Comparison Rules\nYou now have PRE-INJECTION baseline data (captured BEFORE the fault was injected). This is MORE RELIABLE than your first post-injection observation because the baseline values are guaranteed to be unaffected by the fault.\nRules:\n- Compare post-injection metrics against the pre-injection baseline above, NOT against your first post-injection check.\n- A significant change from baseline (e.g., disk 10%→13%, CPU 100m→800m, RestartCount 7→8) is STRONG evidence the fault is in effect.\n- If metrics are SIMILAR to baseline, the fault may not be working.\n\n**FORMAT REQUIREMENT (when Pre-Injection Baseline is available)**:\nFor steps that decide whether the injection took effect, include baseline comparison in the evidence in the format:\n  \"baseline: <metric from above> → post-injection: <metric you observe NOW> (Δ<change>)\"\nSteps without a baseline comparison weaken their own evidence. Propagated-effect steps (OOM, latency, business impact) may omit it — mark them 'expected'/'not_applicable' when no observation is in hand.\nIn your VERIFICATION_RESULT, set BaselineUsed: true.\n"
      }
    ]
  },
  {
    "kind": "baseline",
    "label": "bl-network-dns",
    "out": [
      {
        "type": "AIMessage",
        "content": ""
      },
      {
        "type": "ToolMessage",
        "content": "Pre-injection baseline collected at 2026-05-09T10:00:00 (strategy: registry, 1/1 succeeded).\n\nThese metrics were captured BEFORE fault injection using the same kubectl commands you would use for verification. This is your authoritative reference — compare every post-injection observation against these values using delta format: \"baseline: X → current: Y (ΔZ)\".\n\n### df -h\nCommand: `kubectl exec ... df -h`\n```\nFilesystem Size Used Avail Use% Mounted on\n/dev/vda3 40G 6G 34G 16% /host\n/dev/vdb 20G 8G 12G 42% /\n```"
      },
      {
        "type": "AIMessage",
        "content": ""
      },
      {
        "type": "ToolMessage",
        "content": "### Baseline Comparison Rules\nYou now have PRE-INJECTION baseline data (captured BEFORE the fault was injected). This is MORE RELIABLE than your first post-injection observation because the baseline values are guaranteed to be unaffected by the fault.\nRules:\n- Compare post-injection metrics against the pre-injection baseline above, NOT against your first post-injection check.\n- A significant change from baseline (e.g., disk 10%→13%, CPU 100m→800m, RestartCount 7→8) is STRONG evidence the fault is in effect.\n- If metrics are SIMILAR to baseline, the fault may not be working.\n\n**FORMAT REQUIREMENT (when Pre-Injection Baseline is available)**:\nFor steps that decide whether the injection took effect, include baseline comparison in the evidence in the format:\n  \"baseline: <metric from above> → post-injection: <metric you observe NOW> (Δ<change>)\"\nSteps without a baseline comparison weaken their own evidence. Propagated-effect steps (OOM, latency, business impact) may omit it — mark them 'expected'/'not_applicable' when no observation is in hand.\nIn your VERIFICATION_RESULT, set BaselineUsed: true.\n"
      }
    ]
  }
]
