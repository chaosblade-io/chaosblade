sudo: required

dist: xenial

services: 
    - docker

env:
    - CHANGE_MINIKUBE_NONE_USER=true

language: go

go:
    - 1.12.x

before_script:
    -pwd
    -cd $GOPATH
    -ls
    - echo $GOPATH
    - cd $GOPATH
    - mkdir -p src/github.com/chaosblade-io/
    - echo $TRAVIS_BUILD_DIR
    - cp -r $TRAVIS_BUILD_DIR/. $GOPATH/src/github.com/chaosblade-io
    - cd $GOPATH/src/github.com/chaosblade-io
    #- curl -Lo kubectl https://storage.googleapis.com/kubernetes-release/release/v1.15.0/bin/linux/amd64/kubectl && chmod +x kubectl && sudo mv kubectl /usr/local/bin/
    #- curl -Lo minikube https://storage.googleapis.com/minikube/releases/latest/minikube-linux-amd64 && chmod +x minikube && sudo mv minikube /usr/local/bin/
    #- sudo minikube start --vm-driver=none --kubernetes-version=v1.15.0
    #- minikube update-context
    #- JSONPATH='{range .items[*]}{@.metadata.name}:{range @.status.conditions[*]}{@.type}={@.status};{end}{end}'; until kubectl get nodes -o jsonpath="$JSONPATH" 2>&1 | grep -q "Ready=True"; do sleep 1; done
    
script:
    #- kubectl cluster-info
    - go test -v ./... -count=1
    #- make